---
summary: "The npm tarball excludes the .mcpb bundle that bun run bundle writes into dist/, so a bundle built before publishing is never packed."
breaking: false
security: false
---

# 0.5.2 — 2026-09-25

## Changed

- **`package.json` `files` excludes `dist/*.mcpb`**, so the Claude Desktop bundle from `bun run bundle` is never packed into the npm tarball; matches the mcp-ts-core scaffold template.
